
#include "../push_swap.h"

void sort4(t_list **stA, t_list **stB)
{
  indexing(a);
  indexing(b);
  set_target_node(stA, stB);

  


  /*  partiendo del indice 1 el numero del indice en el stackB y el indice en el stackA me mostarian el numero de movimientos que hay que hacer para colocar ese nodo donde tiene que ir
   *  necesitamos saber si esta sobre la media o debajo de la media para poder saber si se usa ra o rra para subirlo y acomodarlo, luego hacer el push. almacenar los movimientos en un puntero
   *  de manera que podamos compararlos entre ellos y saber si hay movimientos en conjunto que se puedan hacer para optimizar el push swap.
   *  
   *  utilizaremos el target node para saber hacia donde lo debemos pushear de manera efectiva.
   *
   *
   *
   *
   *
   *
   *
   *
   */
  
}
